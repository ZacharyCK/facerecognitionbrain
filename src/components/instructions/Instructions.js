import React from 'react';

const Instructions = () => {
  return (
    <div>
      <p className='f3'>
        {'To try this app, get the URL of an image and place it in the input box above'}
      </p>
      <p className='f3'>
        {'Search for an image on your favorite search engine and copy the address(URL)'}
      </p>
    </div>
  );
}

export default Instructions;
